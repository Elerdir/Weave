{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Oprávnění oken Weave (eventy, dialog, notifikace, otevírání odkazů). POZOR: každé nové okno sem musí přibýt, jinak mu Tauri odmítne core pluginy — vlastní #[tauri::command] fungují i bez toho, takže se výpadek projeví až u listen()/dialogu a vypadá jako zamrzlé UI.",
  "windows": [
    "main",
    "settings",
    "logs",
    "gallery",
    "gallery-detail-*",
    "subjects"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission",
    "notification:allow-notify",
    "shell:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
